import { DESIGN_SYSTEM } from 'src/common/designSystem'

export const styles = {
    progressBarContainer: {
        height: 20,
        border: DESIGN_SYSTEM.border,
        borderRadius: 25,
        display: 'flex',
        overflow: 'hidden',
        width: 200,
    },
}
